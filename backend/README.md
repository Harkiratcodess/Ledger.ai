# ledger-security

Local CLI and MCP gateway for **Ledger** — a security runtime for AI coding agents.

```bash
npm install
npm link
ledger --help
ledger init
ledger protect .
ledger exec "npm test"
ledger mcp
```

See the repository root `README.md` for architecture, security model, limitations, and tests.
